<Window x:Class="MirrorVault.Views.ConfigurationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MirrorVault Setup"
        Height="320"
        Width="600"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Loaded="ConfigurationWindow_Loaded">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            Text="Source Folder"
            FontWeight="Bold"/>

        <Grid Grid.Row="1" Margin="0,8,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="110" />
            </Grid.ColumnDefinitions>

            <TextBox
        x:Name="SourceFolderTextBox"
        Height="30"
        VerticalAlignment="Center"
        IsReadOnly="True"/>

            <Button
    Grid.Column="1"
    Margin="10,0,0,0"
    Height="30"
    Content="Browse"
    Click="BrowseButton_Click"/>
        </Grid>

        <TextBlock
            Grid.Row="2"
            Text="Backup Drive"
            FontWeight="Bold"/>

        <ComboBox
            x:Name="BackupDriveComboBox"
            Grid.Row="3"
            Height="30"
            VerticalAlignment="Top"/>

        <Button
            Grid.Row="4"
            Content="Save"
            Width="100"
            Height="35"
            HorizontalAlignment="Right"
            Margin="0,20,0,0"
            Click="SaveButton_Click"/>

    </Grid>

</Window>